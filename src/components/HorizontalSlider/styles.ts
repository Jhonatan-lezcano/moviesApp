import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  sliderContainer: {
    backgroundColor: 'green',
  },
  title: {
    fontSize: 30,
    fontWeight: 'bold',
    color: 'black',
    marginLeft: 15,
  },
});
